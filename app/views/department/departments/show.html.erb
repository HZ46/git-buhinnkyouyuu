<div class="text-center">
  <% if flash[:notice] %>
    <p class="text-active"><%= flash[:notice] %></p>
  <% end %>
  <center>
	<h2>部門ページ(<%= @department.name %>)</h2>
		
		<h3>ブックマーク一覧</h3>
		<table>
	    <% @bookmarks.each do |bookmark| %>

	  <tr>
	    <td><%= link_to bookmark.item.name,item_path(bookmark.item) %></td>
	  </tr>
	    <% end %>
	  </tbody>
	</table><br><br>
	<div class="flex">
		<%= link_to "注文履歴一覧", orders_path, class: "btn btn-primary ml2" %>
	</div>
	</center>
</div>